<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @notification.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @notification.body %>
</p>


<%= link_to 'Edit', edit_notification_path(@notification) if logged_in?(:admin)%>
<%= link_to 'Back', admin_path if logged_in?(:admin)%>
<%= link_to 'Back', client_path if logged_in?(:client)%>
